import { FoundationPlaceholder } from "@/components/shared/foundation-placeholder";

export default function FoundationPage() {
  return (
    <FoundationPlaceholder
      eyebrow="Phase 2.3A"
      title="FrontOS Foundation Ready."
      description="The project architecture, design tokens, and quality tooling are configured."
    />
  );
}
